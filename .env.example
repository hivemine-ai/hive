# Hive runtime configuration — placeholder.
# Populated by PRY-008 (Deployment) with all HIVE_* variables required to run the server.
# Copy this file to .env and fill in real values for local development.
# .env is gitignored and must NEVER be committed.
