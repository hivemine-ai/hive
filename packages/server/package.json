{
  "name": "@hive/server",
  "version": "0.1.0-dev",
  "private": true,
  "description": "Hive MCP server, domain logic, and persistence layer.",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src",
    "test": "echo \"no tests yet\" && exit 0"
  }
}
