{
  "name": "@hive/client",
  "version": "0.1.0-dev",
  "private": true,
  "description": "Reusable client library for Hive (used by tests and Node-side agents).",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src",
    "test": "echo \"no tests yet\" && exit 0"
  }
}
